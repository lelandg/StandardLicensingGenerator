<Window x:Class="KeyPairGeneratorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Key Pair Generator" Height="300" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Key Size:" VerticalAlignment="Center"/>
        <ComboBox x:Name="KeySizeBox" Grid.Row="0" Margin="70,0,0,0" Width="100">
            <ComboBoxItem Content="2048"/>
            <ComboBoxItem Content="3072"/>
            <ComboBoxItem Content="4096"/>
        </ComboBox>
        <Button Content="Generate Key Pair" Grid.Row="1" Margin="0,10,0,0" Width="150" Click="GenerateKeyPair_Click"/>
        <TextBox x:Name="ResultBox" Grid.Row="2" Margin="0,10,0,0" Height="Auto" IsReadOnly="True" TextWrapping="Wrap"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save Private Key" Margin="0,0,10,0" Click="SavePrivateKey_Click"/>
            <Button Content="Save Public Key" Margin="0,0,10,0" Click="SavePublicKey_Click"/>
            <Button x:Name="InsertButton" Content="Insert" IsEnabled="False" Click="InsertButton_Click"/>
        </StackPanel>
    </Grid>
</Window>

